import type { TagColor } from '../types/board.ts';

export const TAG_COLORS: Record<TagColor, string> = {
  "high priority": 'bg-[#F25252] text-black',
  "medium priority": 'bg-[#F2CF63] text-black',
  "low priority": 'bg-[#BBD98F] text-black',
};
